#include "main.h"

/**
 * _pus - pus
 * @str: str
 * Return:0 Success
 */
int _pus(char *str)
{
	int i = 0, b = 0, s;
	char *x = "(null)";

	if (!str)
	{
		for (s = 0 ; s < 6 ; s++)
			puchr(x[s]);
		return (s);
	}
	while (str[i] != '\0')
	{
		puchr(str[i]);
		b++;
		i++;
	} return (b);
}
